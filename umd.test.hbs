{{!--

UMD template for tests.

Used to generate a version that is always exported via CommonJS
and thus can be tested with mocha.

This is done so that the correct coverage report is created.

--}}

{{{code}}}

module.exports = {{{objectToExport}}};